#include "main.h"
/**
 * _islower - checks lowercase
 *
 * c- c is the input character
 * Return : 1 if lowercase
 * Return : 0 if not
 */
int _islower(int c)
{

(if c >= "a" && c <= "z")

{
	return (1);
else
{
	return (0);
}
}
